{% extends "base.twig" %}

{% block content %}
    <div class="container">
        <div class="row">
           <div class="col-12">
               {{ post.content }}
           </div>
           <div class="col-12 {{ exp_mobile() ? 'gx-0' : '' }}">
               {% include 'blocks/layout/account.twig' %}
           </div>
        </div>
    </div>
{% endblock %}